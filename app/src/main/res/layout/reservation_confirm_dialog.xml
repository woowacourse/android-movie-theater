<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="listener"
            type="woowacourse.movie.presentation.view.reservation.seat.SeatSelectionContract.ViewActions" />
    </data>

    <LinearLayout
        android:id="@+id/reservationConfirmDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/size_24">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/reservation_confirm_message"
            android:textSize="@dimen/size_16" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="@dimen/size_45" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/cancelButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clickable="true"
                android:focusable="true"
                android:onClickListener="@{()->listener.onDialogCancelButtonClicked()}"
                android:text="@string/cancel_text"
                android:textColor="@color/grey" />

            <Space
                android:layout_width="@dimen/size_20"
                android:layout_height="match_parent" />

            <TextView
                android:id="@+id/confirmButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clickable="true"
                android:focusable="true"
                android:onClickListener="@{()->listener.onDialogAcceptButtonClicked()}"
                android:text="@string/reservation_finish_text"
                android:textColor="@color/purple_500" />

        </LinearLayout>

    </LinearLayout>

</layout>
