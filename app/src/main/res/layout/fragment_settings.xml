<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_push_notification"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/push_notification"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <TextView
            android:id="@+id/tv_push_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/push_description"
            app:layout_constraintTop_toBottomOf="@id/tv_push_notification"
            android:textSize="12sp"
            android:textColor="#666666" />

        <androidx.appcompat.widget.SwitchCompat
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_push_notification"
            app:layout_constraintBottom_toBottomOf="@id/tv_push_description"
            app:trackTint="@color/selector_notification_track_color"
            app:thumbTint="@color/selector_notification_thumb_color" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

