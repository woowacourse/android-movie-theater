<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="movieScreeningInfoByTheaters"
            type="woowacourse.movie.model.theater.MovieScreeningInfoByTheater" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="8dp">

        <TextView
            android:text="@{movieScreeningInfoByTheaters.screeningInfo.theater.name}"
            android:id="@+id/theater_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="선릉극장"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:layout_marginStart="16dp" />

        <TextView
            android:text="@{@string/theater_movie_screening_time_count(String.valueOf(movieScreeningInfoByTheaters.screeningInfo.screeningTimes.size()))}"
            android:id="@+id/theater_screening_time_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/theater_name"
            app:layout_constraintStart_toStartOf="@+id/theater_name"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="3개의 상영 시간"
            android:textColor="@color/gray_5F5F5F"
            android:textSize="12sp" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/chevron_right"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_margin="8dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>