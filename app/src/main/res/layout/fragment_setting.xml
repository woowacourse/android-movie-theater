<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="listener"
            type="woowacourse.movie.setting.SwitchListener" />
        <variable
            name="checked"
            type="Boolean" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/tv_push_alarm_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:text="@string/push_alarm_setting_title"
            android:textSize="16sp"
            android:textStyle="bold"
            />
        <TextView
            android:id="@+id/tv_push_alarm_caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/tv_push_alarm_title"
            app:layout_constraintStart_toStartOf="@id/tv_push_alarm_title"
            android:text="@string/push_alarm_setting_caption"
            android:layout_marginTop="8dp"
            android:textSize="12sp"/>
        <Switch
            android:id="@+id/sw_push_alarm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:onCheckedChanged="@{(switch, checked) -> listener.onClick()}"
            android:checked="@{checked}"
            android:layout_marginEnd="16dp"
            android:layout_marginTop="26dp"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
