<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/tv_setting_alert_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:fontFamily="@font/roboto_bold"
            android:text="푸시 알림 수신"
            android:textColor="@color/black"
            android:textSize="16sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_setting_alert_guide"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="해제하면 푸시 알림을 수신할 수 없습니다."
            app:layout_constraintStart_toStartOf="@id/tv_setting_alert_title"
            app:layout_constraintTop_toBottomOf="@id/tv_setting_alert_title" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_setting_alert"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:checked="true"
            android:drawableTint="@color/purple"
            app:layout_constraintBottom_toBottomOf="@id/tv_setting_alert_guide"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_setting_alert_title" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
