<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="advertisement"
            type="woowacourse.movie.domain.reservation.Advertisement" />

        <variable
            name="srcDrawable"
            type="int" />
    </data>

    <ImageView
        android:id="@+id/iv_item_advertisement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:contentDescription="@{advertisement.description}"
        app:srcDrawable="@{srcDrawable}" />

</layout>