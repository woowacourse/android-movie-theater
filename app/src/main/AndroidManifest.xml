<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MovieApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie"
        tools:targetApi="31">
        <activity
            android:name=".presentation.view.MoviesActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.view.reservation.detail.ReservationDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.reservation.seat.SeatSelectActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.reservation.complete.ReservationCompleteActivity"
            android:exported="false"
            android:parentActivityName=".presentation.view.MoviesActivity" />

        <receiver
            android:name=".presentation.alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="woowacourse.movie.ALARM" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
