<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name="Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie"
        tools:targetApi="31">
        <receiver android:name=".broadcast.bookingnotificaiotn.BookingAlarmReceiver"/>
        <activity
            android:name=".presentation.view.main.home.seatpick.SeatPickerActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.main.home.bookcomplete.BookCompleteActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.main.home.moviedetail.MovieDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.main.home.MovieListFragment"
            android:exported="true"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
